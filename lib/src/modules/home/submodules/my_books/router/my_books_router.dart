import 'package:flutter/material.dart';

import '../ui/container/my_books_container.dart';

class MyBooksRouter extends StatelessWidget {
  const MyBooksRouter({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const MyBooksContainer();
  }
}
